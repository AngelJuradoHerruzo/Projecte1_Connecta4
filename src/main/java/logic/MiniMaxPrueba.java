/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package logic;

import entities.Casella;

/**
 *
 * @author angel
 */
public class MiniMaxPrueba {
    /*
    public void testMinimaxArbolSencillo() {
        // Crear nodos hoja con valores manuales
        Node hojaA1 = new Node(null); hojaA1.setValor(3);
        Node hojaA2 = new Node(null); hojaA2.setValor(5);
        Node hojaB1 = new Node(null); hojaB1.setValor(2);
        Node hojaB2 = new Node(null); hojaB2.setValor(9);

        // Nodos intermedios
        Node nodoA = new Node(null); nodoA.addHijo(hojaA1); nodoA.addHijo(hojaA2);
        Node nodoB = new Node(null); nodoB.addHijo(hojaB1); nodoB.addHijo(hojaB2);

        // Nodo raíz
        Node raiz = new Node(null); raiz.addHijo(nodoA); raiz.addHijo(nodoB);

        MiniMax miniMax = new MiniMax();

        Node mejor = miniMax.minimaxPrueba(raiz, 3, true);

        // Comprobamos que minimax ha seleccionado la mejor hoja para la IA
        System.out.println("Mejor nodo tiene valor: " + mejor.getValor());
    }
     // Método main para ejecutar la prueba
    public static void main(String[] args) {
        MiniMaxPrueba prueba = new MiniMaxPrueba();
        prueba.testMinimaxArbolSencillo();
    }
*/
}